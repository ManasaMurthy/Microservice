#!groovy

node {
cleanWs()
checkout scm
env.WORKSPACE = pwd()
def file = new File("${env.WORKSPACE}/repo.csv")
def values = new String[3]
def lines = file.readLines()
 lines.each { String line ->
  values = line.split(',')
  println line
 }

}
